[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run app_user.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --browser.gatherUsageStats false"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
